% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/BurdenMLE_DN.R
\name{BurdenMLE_DN}
\alias{BurdenMLE_DN}
\title{Fit a BurdenMLE-DN model}
\usage{
BurdenMLE_DN(
  input_data,
  features = NULL,
  component_endpoints = NULL,
  no_cpts = 10,
  grid_size = 10,
  mutvar_est = TRUE,
  max_iter = 10000,
  max_iter_boot = 1000,
  tol = 1e-06,
  prevalence = NULL,
  bootstrap = TRUE,
  bootstrap_samples = NULL,
  n_boot = 100,
  bootstrap_seed = 1L,
  null_sim = FALSE,
  n_null = 100,
  null_seed = 1L,
  return_likelihood = TRUE,
  estimate_posteriors = TRUE,
  estimate_effective_penetrance = TRUE,
  optimizer = c("mixsqp", "EM"),
  mixsqp_control = list()
)
}
\arguments{
\item{input_data}{A data frame with at least two genes; explicit, unique,
nonempty, non-missing gene row names; and either columns \code{case_count} and
\code{expected_count}, or columns \code{case_count}, \code{case_rate}, and \code{N}. Supplied
expected counts are retained exactly. See the repository's "Worked
example" page.}

\item{features}{Optional finite one-hot numeric matrix assigning genes to
nonempty annotation strata. It must have explicit, unique, nonempty,
non-missing gene row names identical to \code{input_data} in the same order and
unique, nonempty, non-missing stratum column names. Reordered feature rows
fail rather than being realigned.}

\item{component_endpoints}{Optional numeric vector of upper endpoints for
the uniform log-rate-ratio mixture components. Use
\code{\link[=effect_size_grid]{effect_size_grid()}} to construct prevalence-derived or conservatively
wide grids. When supplied, these endpoints take precedence and \code{no_cpts}
is ignored.}

\item{no_cpts}{Number of mixture components when \code{component_endpoints} is
not supplied.}

\item{grid_size}{Number of numerical-integration points per component.}

\item{mutvar_est}{Compute mutational variance and annotation summaries.
This uses deterministic analytic moments of the continuous-uniform
components and requires \code{case_rate}; expected-count-only inputs must set
this to \code{FALSE}.}

\item{max_iter, max_iter_boot, tol}{Controls retained for the optional legacy
optimizer.}

\item{prevalence}{Population prevalence on the 0--1 scale. Required when
component endpoints are generated, mutational variance is requested, or
effective penetrance is requested; otherwise it may be \code{NULL}.}

\item{bootstrap}{Run a gene-level nonparametric bootstrap.}

\item{bootstrap_samples}{Optional character matrix of sampled gene IDs, with
one input-gene draw per row and one replicate per column. Repeated IDs are
allowed. Numeric row positions are not accepted.}

\item{n_boot}{Number of bootstrap replicates.}

\item{bootstrap_seed}{Positive integer seed for generated bootstrap samples.
The caller's RNG state is restored. Ignored when \code{bootstrap_samples} is
supplied.}

\item{null_sim}{Run optional parametric null simulations. These are not
needed for estimation or bootstrap confidence intervals.}

\item{n_null}{Number of null simulations.}

\item{null_seed}{Positive integer seed for null simulations. The caller's
RNG state is restored.}

\item{return_likelihood}{Store the maximized log likelihood.}

\item{estimate_posteriors}{Compute gene-level posterior summaries.}

\item{estimate_effective_penetrance}{Compute mutation-weighted effective
penetrance: the average penetrance of an excess case-associated de novo
variant. This requires \code{case_rate}; expected counts alone do not uniquely
determine mutation-rate weights.}

\item{optimizer}{Optimization routine. MixSQP is the supported default.}

\item{mixsqp_control}{Named list passed to \code{mixsqp::mixsqp()}. Package
defaults preserve small components and scale the active-set allowance to
the requested component grid.}
}
\value{
An object of class \code{BurdenMLEDN_fit}. Important fields include
\code{delta}, \code{component_endpoints}, \code{ll}, \code{fit_status},
\code{uncertainty_reliable}, \code{mutvar_output}, \code{penetrance},
\code{bootstrap_output}, \code{null_output}, \code{input_summary}, and
\code{optimizer_elapsed}.
\code{input_summary$sample_size} is \code{NA} when \code{N} was not supplied, and
\code{input_summary$case_rate_available} reports whether explicit mutation rates
were supplied and mutation-rate-dependent estimands are supported.
Stratum-indexed weights, mutational-variance summaries, and confidence
intervals retain the feature-column names.
}
\description{
Fits a discrete-mixture model to gene-level de novo variant counts using
MixSQP. The input contains one row per gene, observed case counts, and either
exact expected counts or the mutation-rate inputs needed to derive them.
}
\details{
Gene-level Poisson p-values are inclusive one-sided probabilities,
\code{P(X >= observed)}. When null simulation and mutational variance are both
requested, the finite-null p-value includes ties and uses a plus-one
correction, so its minimum is \code{1 / (n_null + 1)}.
}
\examples{
example_file <- system.file("extdata", "synthetic_example.csv",
                           package = "BurdenMLEDN")
dat <- read.csv(example_file, row.names = "gene")
fit <- BurdenMLE_DN(
  dat,
  prevalence = 0.02,
  bootstrap = FALSE,
  null_sim = FALSE,
  estimate_effective_penetrance = TRUE
)
fit
}
