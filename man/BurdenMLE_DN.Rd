% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/BurdenMLE_DN.R
\name{BurdenMLE_DN}
\alias{BurdenMLE_DN}
\title{Fit a BurdenMLE-DN model}
\usage{
BurdenMLE_DN(
  input_data,
  features = NULL,
  component_endpoints = NULL,
  no_cpts = 10,
  grid_size = 10,
  mutvar_est = TRUE,
  max_iter = 10000,
  max_iter_boot = 1000,
  tol = 1e-06,
  prevalence = NULL,
  bootstrap = TRUE,
  bootstrap_samples = NULL,
  n_boot = 100,
  null_sim = TRUE,
  n_null = 100,
  return_likelihood = TRUE,
  estimate_posteriors = FALSE,
  estimate_effective_penetrance = TRUE,
  optimizer = c("mixsqp", "EM"),
  mixsqp_control = list()
)
}
\arguments{
\item{input_data}{A data frame with one row per gene and columns
\code{case_count}, \code{case_rate}, and \code{N}. See
the repository's "Worked example" page.}

\item{features}{Optional one-hot numeric matrix assigning genes to
annotation strata. Row names and order must match \code{input_data}.}

\item{component_endpoints}{Optional numeric vector of upper endpoints for
the uniform log-rate-ratio mixture components.}

\item{no_cpts}{Number of mixture components when \code{component_endpoints} is
not supplied.}

\item{grid_size}{Number of numerical-integration points per component.}

\item{mutvar_est}{Compute mutational variance and annotation summaries.}

\item{max_iter, max_iter_boot, tol}{Controls retained for the optional legacy
optimizer.}

\item{prevalence}{Population prevalence on the 0--1 scale.}

\item{bootstrap}{Run a gene-level nonparametric bootstrap.}

\item{bootstrap_samples}{Optional matrix of user-supplied bootstrap row
indices.}

\item{n_boot}{Number of bootstrap replicates.}

\item{null_sim}{Run parametric null simulations.}

\item{n_null}{Number of null simulations.}

\item{return_likelihood}{Store the maximized log likelihood.}

\item{estimate_posteriors}{Compute gene-level posterior summaries.}

\item{estimate_effective_penetrance}{Compute effective penetrance.}

\item{optimizer}{Optimization routine. MixSQP is the supported default.}

\item{mixsqp_control}{Named list passed to \code{mixsqp::mixsqp()}. Package
defaults preserve small components and scale the active-set allowance to
the requested component grid.}
}
\value{
An object of class \code{BurdenMLEDN_fit}. Important fields include
\code{delta}, \code{component_endpoints}, \code{ll}, \code{mutvar_output}, \code{penetrance},
\code{bootstrap_output}, and \code{optimizer_elapsed}.
}
\description{
Fits a discrete-mixture model to gene-level de novo variant counts using
MixSQP. The input contains one row per gene and, at minimum, observed case
counts and gene-specific mutation rates.
}
\examples{
example_file <- system.file("extdata", "synthetic_example.csv",
                           package = "BurdenMLEDN")
dat <- read.csv(example_file, row.names = "gene")
fit <- BurdenMLE_DN(
  dat,
  prevalence = 0.02,
  bootstrap = FALSE,
  null_sim = FALSE,
  estimate_effective_penetrance = TRUE
)
fit
}
